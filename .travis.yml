# travis CI config
language: python
python:
  - 2.6
  - 2.7
  - 3.3
  - 3.4
  - 3.5
  - pypy
  - pypy3
cache: false
install:
  - pip install -r requirements.txt
  - pip install codecov coverage
script: nosetests --with-coverage --cover-xml
after_success:
  - codecov
branches:
  only:
    - master
